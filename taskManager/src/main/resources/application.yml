spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/TaskManagerDB  # Updated database name to match Docker
    username: root
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:

        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect


